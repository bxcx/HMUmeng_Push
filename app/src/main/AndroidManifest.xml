<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.hm.umeng.push"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="UMENG_APPKEY"
            android:value="57eaa4d167e58e6a2600437f"/>
        <meta-data
            android:name="UMENG_MESSAGE_SECRET"
            android:value="e2ccc88e0f4451bbbb65397c4a61b16d"/>
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="base"/>

        <!-- 使用完全自定义消息来开启应用的服务进程的示例服务配置 -->
        <!-- 不是必需的服务，可以根据开发者的需求进行设置 -->
        <!--<service android:name=".NotificationService"/>-->
        <!--<service android:name=".MyPushIntentService" />-->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>